public enum RotDirection
{
    Clockwise,
    Counterclockwise
}
public enum DialDirection
{
    Up,
    Right,
    Down,
    Left
}
public enum SectionType
{
    Switch,
    Dial,
    Number,
    Grid
}